@model IEnumerable<Car_Database.Models.GearBox>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GearBoxType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GearBoxType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GearBoxID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GearBoxID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GearBoxID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
