@model IEnumerable<Car_Database.Models.GearBox>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GearBoxType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GearBoxType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GearBoxID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GearBoxID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GearBoxID })
        </td>
    </tr>
}

</table>
